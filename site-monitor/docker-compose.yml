services:
  site-monitor:
    build: .
    networks:
      - backend
    ports:
      - "5050:5050"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5050"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 5s
networks:
  backend:
    driver: bridge
